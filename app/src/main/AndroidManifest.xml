<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diaper_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrgInfoActivity"></activity>
        <activity android:name=".IntroduceActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".EtcActivity" />
        <activity android:name=".Class.EtcActivity" />
        <activity android:name=".CntinfoActivity" />
        <activity android:name=".UserinfoActivity" />
        <activity android:name=".LogModifyActivity" />
        <activity android:name=".StatisticActivity" />
        <activity android:name=".CntAddActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
    </application>

</manifest>